import validate from "validate.js";

describe("validator" , ()=> {
   // it("should create escape html",() => {
   //      const cons = {html : {presence : true};
   //      const result = validate({html : "foo © bar ≠ baz ???? qux"} , cons);
   //      expect(result).toBe("&#x66;&#x6F;&#x6F;&#x20;&copy;&#x20;&#x62;&#x61;&#x72;&#x20;&ne;&#x20;&#x62;&#x61;&#x7A;&#x20;&#x1D306;&#x20;&#x71;&#x75;&#x78;");
   // });
});